{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
    % {config, "config/sys.config"},
    {apps, [cer3bro]}
]}.

{alias, [
    {f, [{fmt, "--write"}]},
    {test, [f, compile, dialyzer, eunit, ct]}
]}.

{project_plugins, [erlfmt]}.
